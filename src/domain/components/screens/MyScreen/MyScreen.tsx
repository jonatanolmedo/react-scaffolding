// HomeScreen.tsx
import React from 'react';
import {  Text, ScrollView } from 'react-native'; // Importa ScrollView

const MyScreen: React.FC = () => {

  return (
    // Use ScrollView as the outer container
    <ScrollView >
      <Text>Empty Screen</Text>
    </ScrollView>
  );
};

export default MyScreen;
